# Kernels check_shaders_compile.sh must find. Below this its scope has collapsed — a worktree with no
# imported .glsl reports a pass it never earned — so falling short is exit 2, not a violation.
#
# It falls when a kernel is legitimately deleted, and the integrator writes it. A lane that has to edit a
# gate in order to delete a kernel will eventually edit the gate instead of deleting the kernel.
8
